class BioDetails {
    constructor(firstName, lastName,middleName, dateOfBirth) {
      this.firstName = firstName;
      this.middleName=middleName;
      this.lastName = lastName;
      this.dateOfBirth = dateOfBirth; // stored in 'DD-MM-YYYY' format
    }
  }
  
  export default BioDetails;
  